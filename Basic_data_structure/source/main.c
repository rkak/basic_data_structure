#include <stdio.h>
#include "queue.h"
#include "stack.h"
#include "pibonacci.h"


int main() {

/*
	// ---------------------- stack side -------------------- 

	_stack_t * s;
	_data_t * pop_data;


	s = stack_initialization();
	get_data(s);
	print_stack(s);
	pop_data = pop(s);

	printf("This is pop data from stack : %d\n", pop_data->num);

	// --------------------- queue side --------------------- 

	_queue_t * q;
	_data_t * get_data;

	q = queue_initialization();
	get_put(q);
	print_queue(q);
	get_data = get(q);

	printf("This is get data from queue : %d\n", get_data->num);

	
	// --------------------- pibonacci side ---------------- 

	_pibonacci_t * pibo;
	pibo = pibonacci_initialization();

	insert_pibonacci(pibo);
	sum_pibonacci(pibo);
	print_pibonacci(pibo);
	print_sum_pibonacci(pibo);

	*/

	// --------------------- Linked list --------------------


	printf("Test");
}
